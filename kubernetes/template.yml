---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: type1
  labels:
    app: type1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: type1
  template:
    metadata:
      labels:
        app: type1
    spec:
      containers:
        - name: type1
          image: username/type1:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 120
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 5


---


apiVersion: v1
kind: Service
metadata:
  name: type1
  labels:
    app: type1
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: type1

